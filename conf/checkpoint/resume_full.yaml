# Full training resume
path: null                    # Will be auto-detected or specified
load_model: true
load_optimizer: true
load_scheduler: true
load_epoch: true
load_metrics: false
strict_loading: true
map_location: null

# Use standard storage config
storage:
  checkpoint_dir: "checkpoints"
  model_dir: "models"
naming:
  best_model_name: "best_model.pth"
  latest_name: "latest.pth"